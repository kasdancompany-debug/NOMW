# Animal placeholder media

Files referenced by the content catalog use names like:

`moose-hero.PLACEHOLDER.webp`

Replace these with final assets and update `MediaAsset.src` / set `placeholder: false` when ready.
Do not invent Indigenous names or scientific claims in filenames.

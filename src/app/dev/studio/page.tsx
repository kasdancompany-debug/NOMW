import { ExhibitStudio } from "@/components/dev/ExhibitStudio";

/**
 * Exhibit development hub — available in preview/production so Vercel builds
 * can jump stations. Not linked from guest attract UI.
 */
export default function ExhibitStudioPage() {
  return <ExhibitStudio />;
}

/**
 * Root URL. Station assignment (localStorage / ?station= / setup screen)
 * is handled client-side by StationGate — do not hard-redirect to Welcome.
 */
export default function HomePage() {
  return null;
}
